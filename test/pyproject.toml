[project]
name = "demo"
version = "1.0.0"
dependencies = [
    'requests',
]

[project.scripts]
demo = "demo:main [icon=python.ico]"

[project.gui-scripts]
demog = "demo:gui [icon=python.ico]"

[tool.winbundle]
src = [
    'demo',
]
need_tkinter = true